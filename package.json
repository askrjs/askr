{
  "name": "@askrjs/monorepo",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Askr monorepo",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces --if-present build",
    "test": "npm run --workspaces --if-present test",
    "lint": "npm run lint:root && npm run --workspaces --if-present lint",
    "lint:root": "prettier --check .",
    "fmt": "npm run fmt:root && npm run --workspaces --if-present fmt",
    "fmt:root": "prettier --write .",
    "dev": "npm run --workspace @askrjs/askr dev",
    "inventory": "node scripts/generate-inventory.js",
    "verify:themes": "npm run --workspace @askrjs/askr-themes verify:token-contract && npm run --workspace @askrjs/askr-themes verify:responsive && npm run --workspace @askrjs/askr-themes verify:slots",
    "verify:platform-version": "node scripts/validate-platform-version.js",
    "verify:monorepo": "node scripts/validate-monorepo.js && npm run verify:platform-version && npm run verify:themes"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
